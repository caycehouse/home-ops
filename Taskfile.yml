version: "3"

env:
  # Directories
  ANSIBLE_DIR: "{{.ROOT_DIR}}/ansible"
  # Binaries
  PYTHON_BIN: python3

tasks:
  default: task -l

includes:
  ansible: .taskfiles/Ansible.yml
  workstation: .taskfiles/Workstation.yml
