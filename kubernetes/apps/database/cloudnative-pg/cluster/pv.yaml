---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: cloudnative-pg
spec:
  storageClassName: cloudnative-pg
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  nfs:
    path: /mnt/storage0/nfs_k8s/cloudnative-pg
    server: ${NAS_IP}
