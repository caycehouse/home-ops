---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: weave-gitops
spec:
  defaultBackend:
    service:
      name: weave-gitops
      port:
        number: 80
  ingressClassName: tailscale
  tls:
    - hosts:
        - weave-gitops
