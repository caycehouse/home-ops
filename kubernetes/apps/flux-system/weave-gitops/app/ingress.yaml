---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: weave-gitops
spec:
  defaultBackend:
    service:
      name: weave-gitops
      port:
        number: 9001
  ingressClassName: tailscale
  tls:
    - hosts:
        - weave-gitops
