---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app atm10a
spec:
  chartRef:
    kind: OCIRepository
    name: atm10a
  interval: 1h
  timeout: 30m
  values:
    fullnameOverride: *app
    deploymentAnnotations:
      reloader.stakater.com/auto: "true"
    image:
      repository: ghcr.io/itzg/minecraft-server
      tag: 2026.8.2-java21-graalvm@sha256:aaa4148b64faae6b7e4035bebb9b8161a95ad404949355f2758041dcb3aaf788
      pullPolicy: IfNotPresent
    livenessProbe:
      initialDelaySeconds: 0
      periodSeconds: 10
      failureThreshold: 18
      timeoutSeconds: 5
    readinessProbe:
      initialDelaySeconds: 0
      periodSeconds: 10
      failureThreshold: 3
      timeoutSeconds: 5
    startupProbe:
      # chart only templates failureThreshold/periodSeconds here
      enabled: true
      failureThreshold: 90
      periodSeconds: 10
    serviceAnnotations:
      external-dns.alpha.kubernetes.io/hostname: &hostname atm10a.thefryer.games
      external-dns.alpha.kubernetes.io/target: mc-router.thefryer.games
      gatus.home-operations.com/endpoint: |-
        group: services
      mc-router.itzg.me/externalServerName: *hostname
    minecraftServer:
      difficulty: "hard"
      eula: "true"
      forcegameMode: true
      icon: "https://media.forgecdn.net/avatars/thumbnails/1992/863/256/256/639226851425896647.png"
      maxPlayers: 6
      maxTickTime: 180000
      memory: "75%"
      motd: "All the Mods 10 Aeronautics | ${MODPACK_VERSION}"
      ops: "FryyourBacon"
      overrideServerProperties: true
      spawnProtection: 1
      type: "AUTO_CURSEFORGE"
      viewDistance: 8
      autoCurseForge:
        # Blank key falls through to the API key baked into the image
        apiKey:
          key: ""
        filenameMatcher: "${MODPACK_VERSION}.zip"
        slug: "all-the-mods-10-aeronautics"
        excludeMods:
          - simple-backups
      rcon:
        enabled: true
        # Image generates a random password per start and writes it where
        # rcon-cli finds it; existingSecret only suppresses the unused
        # chart-managed rcon Secret.
        withGeneratedPassword: true
        existingSecret: &secret atm10a-secret
    persistence:
      dataDir:
        enabled: true
        existingClaim: *app
    extraEnv:
      ALLOW_FLIGHT: "true"
      CF_OVERRIDES_EXCLUSIONS: |-
        shaderpacks/**
      ENABLE_COMMAND_BLOCK: "true"
      ENABLE_ROLLING_LOGS: "true"
      ENFORCE_WHITELIST: "true"
      OP_PERMISSION_LEVEL: "3"
      OVERRIDE_WHITELIST: "true"
      RCON_CMDS_LAST_DISCONNECT: |-
        stop
      RCON_CMDS_STARTUP: |-
        gamerule playersSleepingPercentage 0
      SIMULATION_DISTANCE: "6"
      SNOOPER_ENABLED: "false"
      STOP_DURATION: 180
      STOP_SERVER_ANNOUNCE_DELAY: 60
      SYNC_CHUNK_WRITES: "false"
      USE_MEOWICE_FLAGS: "true"
      USE_MEOWICE_GRAALVM_FLAGS: "true"
    envFrom:
      - secretRef:
          name: *secret
    extraPodSpec:
      terminationGracePeriodSeconds: "300"
    resources:
      requests:
        cpu: 2000m
        memory: &mem 12Gi
      limits:
        memory: *mem
    podSecurityContext:
      runAsUser: 1000
      runAsGroup: 1000
      fsGroup: 1000
      fsGroupChangePolicy: OnRootMismatch
