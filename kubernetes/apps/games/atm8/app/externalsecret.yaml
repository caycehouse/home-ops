---
# yaml-language-server: $schema=https://k8s-skeemahs.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: atm8
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: atm8-secret
    template:
      data:
        CF_API_KEY: "{{ .CF_API_KEY }}"
        RCON_PASSWORD: "{{ .ATM8_RCON_PASSWORD }}"
        WHITELIST: "{{ .WHITELIST }}"
  dataFrom:
    - extract:
        key: curseforge
    - extract:
        key: atm8
    - extract:
        key: minecraft
