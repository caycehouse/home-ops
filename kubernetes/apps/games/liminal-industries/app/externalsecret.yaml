---
# yaml-language-server: $schema=https://k8s-skeemahs.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: liminal-industries
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: liminal-industries-secret
    template:
      data:
        CF_API_KEY: "{{ .CF_API_KEY }}"
        RCON_PASSWORD: "{{ .LIMINAL_INDUSTRIES_RCON_PASSWORD }}"
        WHITELIST: "{{ .WHITELIST }}"
  dataFrom:
    - extract:
        key: curseforge
    - extract:
        key: liminal-industries
    - extract:
        key: minecraft
