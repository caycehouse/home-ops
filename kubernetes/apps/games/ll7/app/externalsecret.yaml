---
# yaml-language-server: $schema=https://k8s-skeemahs.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ll7
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: ll7-secret
    template:
      data:
        CF_API_KEY: "{{ .CF_API_KEY }}"
        RCON_PASSWORD: "{{ .LL7_RCON_PASSWORD }}"
        WHITELIST: "{{ .WHITELIST }}"
  dataFrom:
    - extract:
        key: curseforge
    - extract:
        key: ll7
    - extract:
        key: minecraft
