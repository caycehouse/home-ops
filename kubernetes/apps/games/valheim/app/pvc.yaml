---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: valheim-runtime
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
  storageClassName: ceph-block
