---
# yaml-language-server: $schema=https://k8s-skeemahs.pages.dev/volsync.backube/kopiamaintenance_v1alpha1.json
apiVersion: volsync.backube/v1alpha1
kind: KopiaMaintenance
metadata:
  name: daily-local
spec:
  enabled: true
  trigger:
    schedule: 30 3,15 * * *
  repository:
    repository: volsync-maintenance-secret
