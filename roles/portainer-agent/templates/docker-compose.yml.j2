# {{ ansible_managed }}
---

services:
  portainer_agent:
    image: portainer/agent:2.18.3-alpine@sha256:ced57737c02a68f9c52c25a0c9875eef8900c6eb9884134209f93ad439f38523
    container_name: portainer_agent
    volumes:
      - /var/lib/docker/volumes:/var/lib/docker/volumes
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - 9001:9001
    networks:
      - internal
    restart: always
networks:
  internal:
    driver: bridge
    driver_opts:
      com.docker.network.driver.mtu: 1280
