# Example usage: ansible-playbook state.yml
# This playbook deploys my whole homelab.

---

- hosts: 'all'
  roles:
    - 'common'
    - 'artis3n.tailscale'
  vars:
    tailscale_args: "--ssh"
  vars_files:
    - 'vault.yml'

- hosts: 'cupcakes'
  roles:
    - 'proxmox'
